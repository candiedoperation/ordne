#Install GSettings Schema for Settings Store
install_data (
    join_paths('data', 'gschema.xml'),
    install_dir: join_paths (get_option ('datadir'), 'glib-2.0', 'schemas'),
    rename: meson.project_name () + '.gschema.xml'
)
